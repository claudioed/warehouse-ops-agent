apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-ops-agent.fullname" . }}
  labels:
    {{- include "warehouse-ops-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warehouse-ops-agent.selectorLabels" . | nindent 4 }}
